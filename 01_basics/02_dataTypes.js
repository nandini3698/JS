"use strict" // Instructs engine to treat all code as newer version of JS. Currently by default all code is treated as newer version

// alert( 3+3) /* will give error because we are using node js and not browser */


